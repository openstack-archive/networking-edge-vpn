# If datacenter only has 1 MLX switch: only address, username, password and uplink_ports are required.
# If datacenter has MCT cluster, all fields are required.
# uplink_ports should contain a comma-separated list
[switch_mlx]
address = 
username = 
password = 
#l2vpn_peer_ip = 
uplink_ports = 
#cluster_peer_address =
#cluster_peer_username =
#cluster_peer_password =
#cluster_peer_l2vpn_peer_ip = 
#cluster_peer_uplink_ports =
